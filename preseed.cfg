# Debian 11 Preseed Configuration for Automated Installation

# Locale and Keyboard
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# Network Configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-iso-builder
d-i netcfg/get_domain string local

# Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Clock and Timezone
d-i clock-setup/utc boolean true
d-i time/zone string UTC

# Partitioning
d-i partman-auto/method string regular
d-i partman-auto/disk string /dev/vda
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# User Account
d-i passwd/root-login boolean false
d-i passwd/user-fullname string Builder User
d-i passwd/username string builder
d-i passwd/user-password password builder123
d-i passwd/user-password-again password builder123
d-i user-setup/allow-password-weak boolean true

# Package Selection
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string build-essential openssh-server sudo wget curl git
d-i pkgsel/upgrade select full-upgrade
d-i pkgsel/update-policy select none

# GRUB
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string /dev/vda

# Finish
d-i finish-install/reboot_in_progress note
d-i debian-installer/exit/poweroff boolean true